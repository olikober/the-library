---
description: Get expertise context
---
Use skill:mulch and 
1) Check if directory .mulch exists
2) If yes: execute `ml prime`
   If no: Ask user if expertise tracking shall be initialized.
      yes: execute `ml init` 

Use skill:seeds and 
1) Check if directory .seeds exists
2) If yes: execute `sd prime`
   If no: Ask user if issue tracking shall be initialized.
      yes: execute `sd init` 
